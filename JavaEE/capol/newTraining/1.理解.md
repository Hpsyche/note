```java
//
// Source code recreated from a .class file by IntelliJ IDEA
// (powered by Fernflower decompiler)
//

package com.capol.base.service;

import com.capol.base.response.TableResultResponse;
import com.capol.base.utils.QueryPageUtil;
import java.util.List;
import tk.mybatis.mapper.common.Mapper;

public interface IBaseService<M extends Mapper<T>, T> {
    T selectOne(T entity);

    T selectById(Object id);

    List<T> selectList(T entity);

    List<T> selectListAll();

    Long selectCount(T entity);

    void insert(T entity);

    void insertSelective(T entity);

    void delete(T entity);

    void deleteById(Object id);

    void updateById(T entity);

    void updateSelectiveById(T entity);

    List<T> selectByExample(Object example);

    int selectCountByExample(Object example);

    TableResultResponse<T> selectByQuery(QueryPageUtil query);

    TableResultResponse<T> selectByExampleSupportPage(QueryPageUtil query);
}

```

```java
//
// Source code recreated from a .class file by IntelliJ IDEA
// (powered by Fernflower decompiler)
//

package com.capol.base.service;

import com.capol.base.response.TableResultResponse;
import com.capol.base.utils.EntityUtil;
import com.capol.base.utils.QueryPageUtil;
import com.capol.base.utils.StringUtil;
import com.github.pagehelper.Page;
import com.github.pagehelper.PageHelper;
import java.lang.reflect.ParameterizedType;
import java.util.Iterator;
import java.util.List;
import java.util.Map.Entry;
import org.springframework.beans.factory.annotation.Autowired;
import tk.mybatis.mapper.common.Mapper;
import tk.mybatis.mapper.entity.Example;
import tk.mybatis.mapper.entity.Example.Criteria;

public abstract class BaseService<M extends Mapper<T>, T> implements IBaseService<M, T> {
    @Autowired
    protected M mapper;

    public BaseService() {
    }

    public void setMapper(M mapper) {
        this.mapper = mapper;
    }

    public T selectOne(T entity) {
        return this.mapper.selectOne(entity);
    }

    public T selectById(Object id) {
        return this.mapper.selectByPrimaryKey(id);
    }

    public List<T> selectList(T entity) {
        return this.mapper.select(entity);
    }

    public List<T> selectListAll() {
        return this.mapper.selectAll();
    }

    public Long selectCount(T entity) {
        return new Long((long)this.mapper.selectCount(entity));
    }

    public void insert(T entity) {
        EntityUtil.setCreatAndUpdatInfo(entity);
        this.mapper.insert(entity);
    }

    public void insertSelective(T entity) {
        EntityUtil.setCreatAndUpdatInfo(entity);
        this.mapper.insertSelective(entity);
    }

    public void delete(T entity) {
        this.mapper.delete(entity);
    }

    public void deleteById(Object id) {
        this.mapper.deleteByPrimaryKey(id);
    }

    public void updateById(T entity) {
        EntityUtil.setUpdatedInfo(entity);
        this.mapper.updateByPrimaryKey(entity);
    }

    public void updateSelectiveById(T entity) {
        EntityUtil.setUpdatedInfo(entity);
        this.mapper.updateByPrimaryKeySelective(entity);
    }

    public List<T> selectByExample(Object example) {
        return this.mapper.selectByExample(example);
    }

    public int selectCountByExample(Object example) {
        return this.mapper.selectCountByExample(example);
    }

    public TableResultResponse<T> selectByQuery(QueryPageUtil query) {
        Class<T> clazz = (Class)((ParameterizedType)this.getClass().getGenericSuperclass()).getActualTypeArguments()[1];
        Example example = new Example(clazz);
        if (query.entrySet().size() > 0) {
            Criteria criteria = example.createCriteria();
            Iterator var5 = query.entrySet().iterator();

            while(var5.hasNext()) {
                Entry<String, Object> entry = (Entry)var5.next();
                criteria.andLike((String)entry.getKey(), "%" + entry.getValue().toString() + "%");
            }
        }

        Page<Object> result = this.pageHelper(query.getPage(), query.getLimit(), query.getOrderBy());
        List<T> list = this.mapper.selectByExample(example);
        return new TableResultResponse(result.getTotal(), list);
    }

    public TableResultResponse<T> selectByExampleSupportPage(QueryPageUtil query) {
        Example example = (Example)query.get("example");
        if (example == null) {
            throw new NullPointerException("Example is null");
        } else {
            Page<Object> result = this.pageHelper(query.getPage(), query.getLimit(), query.getOrderBy());
            List<T> list = this.mapper.selectByExample(example);
            return new TableResultResponse(result.getTotal(), list);
        }
    }

    private Page<Object> pageHelper(Integer page, Integer limit, String orderBy) {
        return StringUtil.isNotBlank(orderBy) ? PageHelper.startPage(page, limit) : PageHelper.startPage(page, limit, orderBy);
    }
}

```

```java
public interface IFileTypeService extends IBaseService<FileTypeMapper, FileTypeDTO> {
}
```

```java
public class FileTypeServiceImpl extends BaseService<FileTypeMapper, FileTypeDTO> implements IFileTypeService{
}
```

```JAVA
package com.capol.family.manage.common.entity.vo;

import io.swagger.annotations.ApiModelProperty;
import lombok.Data;

import java.io.Serializable;

@Data
public class PageHelperVO implements Serializable {

    private static final long serialVersionUID = 1L;

    @ApiModelProperty("当前分页数")
    private Integer page;

    @ApiModelProperty("当前页显示数量")
    private Integer limit;

    @ApiModelProperty("排序字段")
    private String column;

    @ApiModelProperty("排序规则（desc、asc）")
    private String orderBy;

}
```

## 3CF

其实 附件ID和附件名称在一个表中

## 日期转换

前提：使用DTO接收，返回使用VO

在接收list请求后，进行处理，从数据库获取列表后，返回了data类型，无论是否copy到vo中都会出现变为时间戳的现象，故在dto转vo的时候加一个转换器

```java
package com.capol.training.newemployee.common.helpers.converter;

import com.capol.base.utils.DateUtil;
import net.sf.cglib.core.Converter;

import java.util.Date;

/**
 * @author fuzihao
 * @date 2019/8/7 15:01
 */
public class ObjectFieldConverter implements Converter {
    private ObjectFieldConverter() {
    }

    private static class SingletonInstance {
        private static final ObjectFieldConverter INSTANCE = new ObjectFieldConverter();
    }

    public static ObjectFieldConverter getInstance() {
        return SingletonInstance.INSTANCE;
    }

    /**
     * value 源对象属性，
     * target 目标对象属性类，
     * context 目标对象setter方法名；
     * <p>
     * 注意：一旦使用Converter，BeanCopier只使用Converter定义的规则去拷贝属性，所以在convert方法中要考虑所有的属性
     *
     * @param value
     * @param target
     * @param context
     * @return
     */
    @Override
    public Object convert(Object value, Class target, Object context) {

        if (value instanceof Date) {
            return DateUtil.DateTimeToString((Date) value);
        } else {
            return value;
        }
    }
}
```

即我们在转换时可以这么使用

```java
@Override
public TableResultResponse<DocumentVO> selectDocumentList(DocumentVO vo) {
    PageInfo<DocumentDTO> pageInfo;
    PageHelper.startPage(vo.getPage(),vo.getLimit());
    System.out.println(vo.getKeyword());
    List<DocumentDTO> list=documentMapper.selectByKeywordAndGroup(vo.getKeyword(),vo.getGroupId());
    pageInfo=new PageInfo<>(list);
    List<DocumentVO> documentVOList=new ArrayList<>();
    try {
        documentVOList=TrainingBeanUtils.dtoListToVoList(pageInfo.getList(),DocumentVO.class);
    } catch (Exception e) {
        e.printStackTrace();
    }
    return new TableResultResponse<>(pageInfo.getTotal(),documentVOList);
}
```

